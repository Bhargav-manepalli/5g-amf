name: Basic - On Push to Main
on:
  push:
    branches:
      - main
jobs:
  basic-job:
    runs-on: ubuntu-latest
    steps:
      - name: Print trigger info
        run: |
          echo "✅ Workflow triggered!"
          echo "Repository: $GITHUB_REPOSITORY"
          echo "Branch: $GITHUB_REF"
          echo "Commit: $GITHUB_SHA"
          echo "Actor: $GITHUB_ACTOR"
